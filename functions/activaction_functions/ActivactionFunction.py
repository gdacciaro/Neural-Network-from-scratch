from abc import abstractmethod

class ActivactionFunction:

    @staticmethod
    @abstractmethod
    def function(input): ##<---- Non so come chiamarla
        pass

    @staticmethod
    @abstractmethod
    def first_derivative(input): ##<---- Non so come chiamarla
        pass
